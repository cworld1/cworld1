---
import { cn } from '@/utils'

const { as: Tag = 'span', class: className } = Astro.props
---

<Tag
  class={cn(
    'bg-primary-foreground rounded text-transparent hover:text-inherit transition-colors',
    className
  )}
>
  <slot />
</Tag>
